extends ../templates/layout

block content 
    h3 Borrar un libro
        div 
            form(method="post") 
                label(for="nombre") Nombre:
                input(type="text", name="nombre", value=`${clientes.nombre}`,disabled)
                
                br
                label(for="correo") Fecha Publicacion:
                input(type="text", name="correo", value=`${clientes.correo}`,disabled)
                
                br
                input(type="hidden", name="id", value=`${clientes.id}`)
                input(type="submit", value="Enviar")